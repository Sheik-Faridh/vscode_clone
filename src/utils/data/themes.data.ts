import { Theme } from '@models';

export const themes: { name: string; value: Theme }[] = [
  { name: 'Light+ (default light)', value: 'light' },
  { name: 'Dark+ (default dark)', value: 'dark' },
];
